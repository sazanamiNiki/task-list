{
  "mcpServers": {
    "task-workflow": {
      "command": "node",
      "args": ["/path/to/task-list/dist/mcp/index.js"],
      "description": "Task Monitor TUI - セッション対応タスク管理システム（tasks.jsonは自動的にtask-listルートに保存されます）"
    }
  }
}
